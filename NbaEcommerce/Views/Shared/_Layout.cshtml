<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - NbaEcommerce</title>

    <script src="~/lib/jquery/dist/jquery.js"></script>
    <script src="~/dist/main.bundle.js"></script>
    <link href="~/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.7.2/css/all.css" integrity="sha384-fnmOCqbTlWIlj8LyTjo7mOUStjsKC4pOpQbqyi7RrhN7udi9RwhKkMHpvLbHG9Sr" crossorigin="anonymous">
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <a class="navbar-brand" href="#">
                <div class="border border-light rounded" style="height:40px;width:40px;"> <h6 class="text-light" style="margin-top:10px">NBA</h6></div>
            </a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarColor03">
                <ul class="navbar-nav mr-auto">
                    @*@{
        if (User.IsInRole("Admin"))
        {*@
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="Prodotto" asp-action="IndexCliente">
                            Catalogo online (cliente)
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="Prodotto" asp-action="Index">
                            Prodotti
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="Marchio" asp-action="Index">
                            Marchi
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="Categoria" asp-action="Index">
                            Categorie
                        </a>
                    </li>
                    @*}
            else
            {
                <li class="nav-item">
                    <a class="nav-link" href="#">Catalogo</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">Azienda</a>
                </li>
            }
        }*@
                </ul>
            </div>
            <form class="form-inline my-2 my-lg-0">
                @if (User.IsInRole("Cliente"))
                {
                    <a href="#" class="nav-link text-white"><i class="fas fa-shopping-cart "></i></a>
                }
            </form>
        <partial name="_LoginPartial" />
        </nav>
    </header>

    <div class="container">
        <partial name="_CookieConsentPartial" />
        <main role="main" class="pb-3">
            @RenderBody()
        </main>
    </div>

    @*<footer class="footer mt-auto py-3 text-center bg-light">
        <div class="container">

            Informazioni generali sul sito e sulla privacy.
            metodi di pagamento accettati.
            condizioni generali.

        </div>
    </footer>*@

    @RenderSection("Scripts", required: false)
</body>

</html>
